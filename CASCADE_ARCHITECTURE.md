# Cascade VPN - Architecture & Client Applications Guide

## 🎯 Архитектура Cascade VPN

### Два режима работы

```
РЕЖИМ 1: Локальная установка
═══════════════════════════════
    sudo bash <(curl -s https://raw.githubusercontent.com/adminbk/cascade-vpn-universal/main/cascade-vpn)
                    ↓
        Выбор: [1] Install VPN on this server
                    ↓
        VPN устанавливается на текущем сервере
                    ↓
        Клиенты подключаются напрямую к этому серверу


РЕЖИМ 2: Каскадная установка (Port Forwarding)
═════════════════════════════════════════════════
    sudo bash <(curl -s https://raw.githubusercontent.com/adminbk/cascade-vpn-universal/main/cascade-vpn)
                    ↓
        Выбор: [2] Setup port forwarding (Cascade to remote server)
                    ↓
        Запрос: IP, username, password основного сервера
                    ↓
        SSH подключение к основному серверу
                    ↓
    ┌─────────────────────────────────────────────────┐
    │  На основном сервере:                           │
    │  • Устанавливается выбранный VPN                │
    │  • Генерируются конфиги клиентов                │
    │                                                  │
    │  На текущем сервере:                            │
    │  • Настраиваются правила port forwarding         │
    │  • Создается туннель к основному серверу        │
    └─────────────────────────────────────────────────┘
                    ↓
        Клиенты подключаются к локальному серверу
                    ↓
        Локальный сервер → Основной сервер → Интернет
```

---

## 🔐 Поддерживаемые VPN протоколы

### 1️⃣ VLESS + Reality (Рекомендуется)

**Характеристики:**
- ✅ **Современный** - новая архитектура Xray
- ✅ **Быстрый** - минимальные накладные расходы
- ✅ **Скрытный** - трудно обнаружить и блокировать
- ✅ **Reality** - маскировка под обычный HTTPS трафик
- ✅ **Безопасность** - AES-128-GCM шифрование

**Порты:**
- Стандартный: **443** (HTTPS)
- Можно настроить: любой порт

**Параметры для туннелирования:**
```bash
# На локальном сервере (для каскада):
iptables -t nat -A PREROUTING -p tcp --dport 443 \
  -j DNAT --to-destination [основной_сервер]:443
```

**Клиентские приложения:**

| Платформа | Приложение | Ссылка | Возможности |
|-----------|-----------|--------|-----------|
| **Linux** | Xray CLI | https://github.com/XTLS/Xray-core | Консоль, конфиги JSON |
| | V2rayN (Wine) | https://github.com/2dust/v2rayN | GUI через Wine |
| | NekoBox | https://github.com/MatsuriDayo/NekoBoxForAndroid | Портирован на Linux |
| **Windows** | V2rayN | https://github.com/2dust/v2rayN | Полнофункциональный GUI |
| | Clash for Windows | https://github.com/Fndroid/clash_for_windows_pkg | Мощный управления |
| | NekoBox | https://github.com/MatsuriDayo/NekoBoxForAndroid | Кроссплатформа |
| **macOS** | V2rayN | v2rayn.com | GUI |
| | NekoBox | Доступно в App Store | Простой интерфейс |
| **iOS** | Stash | https://stash.ws | VPN + Proxy управление |
| | ShadowRocket | Платное в App Store | Мощный VPN клиент |
| | Quantumult X | Платное в App Store | Advanced routing |
| **Android** | NekoBox | https://github.com/MatsuriDayo/NekoBoxForAndroid | Лучший выбор |
| | V2rayNG | https://github.com/2dust/v2rayNG | Простой и надежный |
| | Clash | https://github.com/MetaCubeX/ClashMetaForAndroid | Powerful |

**Пример конфигурации клиента:**
```json
{
  "outbounds": [
    {
      "protocol": "vless",
      "settings": {
        "vnext": [
          {
            "address": "123.45.67.89",
            "port": 443,
            "users": [
              {
                "id": "uuid-здесь",
                "encryption": "none",
                "flow": "xtls-rprx-vision"
              }
            ]
          }
        ]
      },
      "streamSettings": {
        "network": "tcp",
        "security": "tls",
        "tlsSettings": {
          "serverName": "example.com",
          "reality": {
            "enabled": true,
            "fingerprint": "chrome",
            "serverName": "www.google.com",
            "publicKey": "публичный-ключ-здесь"
          }
        }
      }
    }
  ]
}
```

---

### 2️⃣ WireGuard (Рекомендуется)

**Характеристики:**
- ✅ **Простой** - ~400 строк кода
- ✅ **Быстрый** - минимальные накладные расходы
- ✅ **Встроенный** - поддержка в ядре Linux
- ✅ **Удобный** - простая конфигурация
- ✅ **Универсальный** - работает везде

**Порты:**
- Стандартный: **51820** (UDP)
- Можно настроить: любой UDP порт

**Параметры для туннелирования:**
```bash
# На локальном сервере (для каскада):
iptables -t nat -A PREROUTING -p udp --dport 51820 \
  -j DNAT --to-destination [основной_сервер]:51820
```

**Клиентские приложения:**

| Платформа | Приложение | Ссылка | Возможности |
|-----------|-----------|--------|-----------|
| **Linux** | WireGuard | https://www.wireguard.com | Встроено в ядро |
| | wg-easy | https://github.com/WeeJewel/wg-easy | Web UI интерфейс |
| **Windows** | WireGuard App | https://www.wireguard.com/install | Официальное приложение |
| | TunnelBlick | https://tunnelblick.net | OpenVPN + WireGuard |
| **macOS** | WireGuard App | https://www.wireguard.com/install | Официальное приложение |
| | TunnelBlick | https://tunnelblick.net | OpenVPN + WireGuard |
| **iOS** | WireGuard App | App Store | Официальное приложение |
| **Android** | WireGuard App | Google Play | Официальное приложение |

**Пример конфигурации клиента:**
```ini
[Interface]
PrivateKey = <приватный-ключ>
Address = 10.0.0.2/32
DNS = 1.1.1.1

[Peer]
PublicKey = <публичный-ключ-сервера>
Endpoint = 123.45.67.89:51820
AllowedIPs = 0.0.0.0/0
PersistentKeepalive = 25
```

**Где получить конфиги:**
- На сервере выполнить: `wg show`
- Или через web-интерфейс wg-easy: `http://server:51821`
- Или через команду: `wg genkey | tee privatekey | wg pubkey`

**⚡ Маскировка с udp2raw (опционально):**

Если WireGuard блокируется, используйте `udp2raw` для преобразования UDP трафика в TCP:

На сервере:
```bash
udp2raw -s -l 0.0.0.0:443 -r 127.0.0.1:51820 -k mypassword --cipher xor
```

На клиенте (Linux):
```bash
udp2raw -c -r 123.45.67.89:443 -l 127.0.0.1:51820 -k mypassword --cipher xor &
# Затем подключайтесь к WireGuard на localhost:51820
```

На клиенте (Windows/macOS):
- Установите `udp2raw` ([скачать](https://github.com/wangyu-/udp2raw-tunnel))
- Или используйте приложение, поддерживающее proxying (например, Clash)
- Настройте сокет прокси: `127.0.0.1:51820`
- Импортируйте WireGuard конфиг с `Endpoint = 127.0.0.1:51820`

---

### 3️⃣ OpenVPN

**Характеристики:**
- ✅ **Универсальный** - работает везде
- ✅ **Надежный** - проверенная технология
- ✅ **Гибкий** - много параметров настройки
- ⚠️ **Медленнее** - больше накладных расходов
- ⚠️ **Сложнее** - сложнее в конфигурации

**Порты:**
- Стандартный: **1194** (UDP/TCP)
- Можно настроить: любой порт

**Параметры для туннелирования:**
```bash
# На локальном сервере (для каскада):
iptables -t nat -A PREROUTING -p udp --dport 1194 \
  -j DNAT --to-destination [основной_сервер]:1194
```

**Клиентские приложения:**

| Платформа | Приложение | Ссылка |
|-----------|-----------|--------|
| **Linux** | OpenVPN | https://openvpn.net |
| **Windows** | OpenVPN Connect | https://openvpn.net/client-connect-vpn |
| | TunnelBlick | https://tunnelblick.net |
| **macOS** | TunnelBlick | https://tunnelblick.net |
| | OpenVPN Connect | https://openvpn.net/client-connect-vpn |
| **iOS** | OpenVPN Connect | App Store |
| **Android** | OpenVPN Connect | Google Play |

**Пример конфигурации клиента:**
```ini
client
dev tun
proto udp
remote 123.45.67.89 1194

resolv-retry infinite
nobind

remote-cert-tls server
tls-auth [inline] 1

cipher AES-256-GCM
compress lz4-v2

persist-key
persist-tun

verb 3
mute 20

# Встроенные ключи будут добавлены сервером:
<ca>
... CA сертификат ...
</ca>

<cert>
... Сертификат клиента ...
</cert>

<key>
... Приватный ключ ...
</key>

<tls-auth>
... TLS ключ ...
</tls-auth>
```

**Параметры для туннелирования (Cascade):**
```bash
# На локальном сервере (для каскада):
iptables -t nat -A PREROUTING -p udp --dport 1194 \
  -j DNAT --to-destination [основной_сервер]:1194

# OpenVPN также поддерживает TCP (для очень заблокированных сетей):
iptables -t nat -A PREROUTING -p tcp --dport 1194 \
  -j DNAT --to-destination [основной_сервер]:1194
```

**Генерирование новых клиентов:**
```bash
# На сервере где установлен OpenVPN:
bash /usr/local/bin/manage-openvpn-clients.sh add client_name
bash /usr/local/bin/manage-openvpn-clients.sh config client_name SERVER_IP 1194 udp
bash /usr/local/bin/manage-openvpn-clients.sh list
```

---

## 📋 Сравнение протоколов

| Функция | VLESS+Reality | WireGuard | OpenVPN |
|---------|--------------|-----------|---------|
| **Скорость** | ⚡⚡⚡ Высокая | ⚡⚡⚡ Высокая | ⚡⚡ Средняя |
| **Безопасность** | 🔒🔒🔒 Отличная | 🔒🔒🔒 Отличная | 🔒🔒 Хорошая |
| **Скрытность** | 👁️ Скрыт | 👁️ Видим | 👁️ Видим |
| **Простота** | ⭐⭐ Сложная | ⭐⭐⭐ Очень простая | ⭐⭐ Средняя |
| **Поддержка** | 📱 Windows, Linux, Mobile | 📱 Везде | 📱 Везде |
| **Размер кода** | 📦 Большой | 📦 Маленький | 📦 Средний |

---

## 🚀 Пошаговая установка (Cascade режим)

### Шаг 1: Подготовка локального сервера
```bash
# На локальном сервере (через который идет каскад)
sudo bash <(curl -s https://raw.githubusercontent.com/adminbk/cascade-vpn-universal/main/cascade-vpn)
```

### Шаг 2: Выбор режима
```
Выберите опцию: [2] Setup port forwarding (Cascade to remote server)
```

### Шаг 3: Ввод данных основного сервера
```
Remote server IP address: 192.168.1.100
Remote server username (default: root): root
Remote server password: ****
```

### Шаг 4: Выбор VPN протокола
```
Выберите протокол для основного сервера:
[1] VLESS + Reality (рекомендуется)
[2] WireGuard (простой и быстрый)
[3] OpenVPN (универсальный)
```

### Шаг 5: Настройка port forwarding
```
Локальный порт (по умолчанию совпадает с удаленным): 443
```

### Шаг 6: Получение конфигов клиента
```
Конфиги отправлены на локальный сервер:
  /var/cascade-vpn/clients/
  
Скачайте их клиентскими приложениями
```

---

## 🔧 Ручная настройка Port Forwarding

Если нужна более сложная конфигурация:

```bash
# Включить IP forwarding
echo 1 > /proc/sys/net/ipv4/ip_forward

# SSH туннель
ssh -L 0.0.0.0:443:основной_сервер:443 user@основной_сервер -f -N

# iptables правило
iptables -t nat -A PREROUTING -i eth0 -p tcp --dport 443 \
  -j DNAT --to-destination основной_сервер:443
iptables -t nat -A POSTROUTING -j MASQUERADE

# Сохранить правила
iptables-save > /etc/iptables/rules.v4
```

---

## 📱 Рекомендации по выбору

### Для стабильности и скорости:
👉 **WireGuard** - выбирайте если:
- Нужна максимальная скорость
- Используете Linux/macOS/Windows
- Хотите простоту конфигурации
- Мобильные устройства в сети

### Для скрытности от блокировок:
👉 **VLESS + Reality** - выбирайте если:
- Нужна маскировка под HTTPS трафик
- Опасаетесь блокировок VPN
- Хотите максимальную скрытность
- Готовы к более сложной настройке

### Для совместимости:
👉 **OpenVPN** - выбирайте если:
- Нужна работа везде без исключений
- Используете старые устройства
- Требуется максимальная совместимость

---

## ⚠️ Важные замечания

### Безопасность
- 🔐 Всегда используйте HTTPS для веб-интерфейсов
- 🔐 Меняйте пароли после установки
- 🔐 Используйте SSH ключи вместо пароля где возможно

### Производительность
- ⚡ VLESS+Reality и WireGuard примерно одинаковой скорости
- ⚡ Скорость зависит от сетевого оборудования
- ⚡ Большие порты с использованием одного туннеля медленнее

### Поддержка
- 📞 GitHub Issues: https://github.com/adminbk/cascade-vpn-universal/issues
- 📚 Документация: docs/
- 🆘 Логи: /var/log/cascade-vpn/

---

**Версия**: 1.0.0  
**Дата обновления**: Декабрь 2025
