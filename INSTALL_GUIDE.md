# Cascade VPN Universal - Установка и использование

## 🚀 Быстрый старт

### Предусловия
- **ОС**: Debian/Ubuntu или RedHat/CentOS
- **Требования**: 
  - Root доступ
  - 2+ GB свободной памяти
  - 2+ GB свободного места на диске
  - Интернет соединение

### Установка

1. **Клонировать репозиторий**
```bash
git clone https://github.com/yourusername/cascade-vpn-universal
cd cascade-vpn-universal
```

2. **Запустить установщик**
```bash
sudo bash install.sh
```

3. **Выбрать компоненты**
- Интерактивное меню позволит выбрать необходимые сервисы и модули

## 📋 Компоненты проекта

### VPN Сервисы
- **VLESS + Reality** - Современный скрытный протокол
- **WireGuard** - Быстрый и простой VPN протокол
- **OpenVPN** - Универсальный и надежный VPN
- **3X-UI** - Веб-панель управления Xray/V2Ray (опционально)
- **V2Ray** - Универсальный прокси-сервер (опционально)
- **Xray** - Форк V2Ray с дополнительными функциями (опционально)

### Модули Маскировки Трафика
- **Shadowsocks 2022** - Простая и быстрая маскировка
- **gRPC + TLS** - Маскировка под Google сервисы
- **Trojan Protocol** - Трояны-подобный протокол
- **VLESS WebSocket + TLS** - WS транспорт с шифрованием
- **UDP2RAW** - UDP туннелирование (идеально для WireGuard)

## 🛠 Команды управления

### Установка
```bash
sudo bash install.sh      # Интерактивная установка
```

### Обновление
```bash
sudo bash update.sh       # Обновить компоненты
```

### Удаление
```bash
sudo bash uninstall.sh    # Удалить компоненты
```

## 🔧 Системные сервисы

### Управление сервисами
```bash
# Запустить сервис
sudo systemctl start cascade-vpn
sudo systemctl start 3x-ui
sudo systemctl start wg-easy

# Остановить сервис
sudo systemctl stop cascade-vpn

# Перезагрузить
sudo systemctl restart 3x-ui

# Статус
sudo systemctl status wg-easy

# Включить при загрузке
sudo systemctl enable cascade-vpn
```

### Логирование
```bash
# Просмотр логов
journalctl -u cascade-vpn -f
journalctl -u 3x-ui -f

# Все логи установки
cat /var/log/cascade-vpn/install.log
```

## 📁 Структура директорий

```
/opt/cascade-vpn          # Основные файлы
/etc/cascade-vpn          # Конфигурации
/var/log/cascade-vpn      # Логи
/var/lib/cascade-vpn      # Данные
```

## 🌐 Веб-интерфейсы

### 3X-UI Panel
- **Адрес**: `https://server-ip:8443`
- **Логин**: `admin`
- **Пароль**: Генерируется автоматически (см. в логах)

### WireGuard Easy
- **Адрес**: `http://server-ip:51821`
- **Пароль**: Генерируется автоматически

## 📊 Файлы конфигурации

### Сыпель параметров
```bash
/etc/sysctl.d/99-cascade-vpn.conf     # Оптимизация ядра
/etc/security/limits.d/99-cascade-vpn.conf  # Лимиты файлов
```

### Nginx конфигурация
```bash
/etc/nginx/sites-available/cascade-vpn
```

### Systemd сервисы
```bash
/etc/systemd/system/cascade-vpn.service
/etc/systemd/system/3x-ui.service
/etc/systemd/system/wg-easy.service
```

## 🔒 Безопасность

### SSL сертификаты
```bash
sudo bash install.sh  # Выбрать опцию [12] в меню
```

- **Let's Encrypt** - Автоматическое получение сертификатов
- **Self-Signed** - Самоподписанные сертификаты для тестирования

## ❌ Удаление компонентов

```bash
# Удалить один компонент
sudo bash uninstall.sh    # Выбрать компонент

# Полное удаление
sudo bash uninstall.sh    # Выбрать опцию [5]
```

## 🐛 Диагностика

### Проверка статуса
```bash
# Проверить работающие сервисы
systemctl list-units --type=service | grep cascade

# Проверить открытые порты
sudo netstat -tlnp | grep LISTEN

# Проверить логи ошибок
journalctl -u cascade-vpn -p err
```

### Восстановление
```bash
# Восстановить из бэкапа
tar -xzf /var/backups/cascade-vpn/cascade-vpn-backup-*.tar.gz

# Очистить логи
sudo rm -rf /var/log/cascade-vpn/*
```

## 📚 Дополнительная информация

### Структура проекта
- `utils/` - Вспомогательные функции (логирование, цвета, валидация)
- `core/` - Основные функции (проверки, оптимизация)
- `modules/` - Функциональные модули (сертификаты, маскировка, порты)
- `services/` - VPN сервисы и панели
- `configs/` - Конфигурационные шаблоны
- `docs/` - Документация
- `examples/` - Примеры конфигураций
- `tests/` - Тесты

### Переменные окружения
```bash
CASCADE_VPN_HOME="/opt/cascade-vpn"
CASCADE_VPN_CONFIG="/etc/cascade-vpn"
CASCADE_VPN_LOGS="/var/log/cascade-vpn"
CASCADE_VPN_DATA="/var/lib/cascade-vpn"
CASCADE_VPN_VERSION="1.0.0"
```

## 📝 Лицензия
MIT License - См. файл LICENSE

## 🤝 Поддержка
- Создавайте Issues на GitHub
- Проверяйте логи в `/var/log/cascade-vpn/`
- Читайте документацию в `docs/`

---

**Версия**: 1.0.0  
**Последнее обновление**: Декабрь 2025
